package com.vti.entity;

public class MyMath {
	  public int sum(int a, int b) {
	        return a + b;
	    }

	    public int sum(int c, byte d) {
	        return c + d;
	    }

	    public float sum(int e, float f) {
	        return e + f;
	    }

	    public int sum(byte g, int h) {
	        return g + g;
	    }

	    public int sum(byte i, byte j) {
	        return i + j;
	    }

	    public float sum(byte m, float n) {
	        return m + n;
	    }

	    public float sum(float l, int k) {
	        return l + k;
	    }

	    public float sum(float q, byte r) {
	        return q + r;
	    }

	    public float sum(float t, float p) {
	        return t + p;
	    }
}